fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# LLM providers
openai==1.57.0
anthropic==0.40.0

# Local embedding model (no API key, ONNX-based, light vs. torch)
fastembed==0.4.2

# Vector store client for the standalone RAG pipeline (app/rag, app/vector_store)
qdrant-client==1.12.1

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
